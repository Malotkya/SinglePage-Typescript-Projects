/** /Terminal/Settings/Defaults
 * 
 * These are the default constants that will be used by the app.  If there are
 * settings saved in a cookie those will be used instead.
 * 
 * @author Alex Malotky
 */
import Color from "@/Color";

export default {
    "width": 100,
    "height": 30,
    "font-size": 15,
    "font-color": new Color(0, 255, 0),
    "background-color":  new Color(0, 0, 0)
}